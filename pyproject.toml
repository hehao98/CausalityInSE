[tool.poetry]
name = "causality-in-se"
version = "0.1.0"
description = "Download and analyse SE conference paper metadata for causality research"
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
requests = "^2.31"
litellm = ">=1.30"
python-dotenv = ">=1.0"
